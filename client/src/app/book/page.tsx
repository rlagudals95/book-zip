import Layout from "@/widgets/layout/ui/Layout";


export default function BookDetailPage({
 
}) {
  // URL에서 id 파라미터 가져오기

  
  return (
    <Layout>
      <h1>책 요약 상세 페이지</h1>
      <p>요약 ID:</p>
      {/* 여기에 id를 사용하여 요약 데이터를 가져오는 로직 추가 */}
    </Layout>
  );
}